import { CountMetricPipe } from './count-metric.pipe';

describe('CountMetricPipe', () => {
  it('create an instance', () => {
    const pipe = new CountMetricPipe();
    expect(pipe).toBeTruthy();
  });
});
